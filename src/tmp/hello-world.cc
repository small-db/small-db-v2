#include <cmath>
#include <iostream>
#include <optional>
#include <ranges>

int main() {
    std::cout << "Hello, World!" << std::endl;

    // test features from C++17
    // std::optional<int> opt;

    // // test features from C++20
    // std::ranges::iota_view<int> range(0, 10);

    return 0;
}