{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build Example (Debug)",
            "type": "shell",
            "command": "bazel build //src/server:main -c dbg",
            "group": {
                "kind": "build",
                "isDefault": true
            },
        }
    ]
}